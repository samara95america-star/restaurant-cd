apiVersion: v1
kind: Service
metadata:
  name: restaurant-company-service

spec:
  type: LoadBalancer

  selector:
    app: restaurant-company

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
